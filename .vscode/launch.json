{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "EasyToVibeをデバッグ起動",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}